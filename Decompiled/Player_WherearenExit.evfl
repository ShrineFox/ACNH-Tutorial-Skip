flow Root():
    if EventFlowSystemActor.WherearenOrderStatusEventFlowSystemActor.WherearenOrderStatus() in (0, 1, 3, 4, 5, 6, 7):
        # I shouldn't leave without notice.
        Player.OpenMessageWindowPlayer.OpenMessageWindow('TalkSys/SYS_Player:1306_01', true)
    else:
        # I'm still on the clock. I shouldn't leave without saying something.
        Player.OpenMessageWindowPlayer.OpenMessageWindow('TalkSys/SYS_Player:1306', true)
    Player.PlayerDemoMovePlayer.PlayerDemoMove('cNextInfoAngle', 1)
 
